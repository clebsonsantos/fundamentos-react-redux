import React from 'react'; // sempre importar o react para que seja possivel usar jsx

import ReactDOM from 'react-dom'; // utilizar para que a aplicação se comunique via DOM

import App from './App'



ReactDOM.render(

  <App/>,
    document.getElementById('root')
   
  )
